<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet access සඳහා -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Background එකේ දිගටම දුවන්න අවශ්‍ය permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- App එකේ ප්‍රධාන සැකසුම් සහ Icon එක -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.PingBooster">

        <!-- App එක Open කරද්දී එන Main UI එක -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Phone එක sleep වුණත් Ping එක යවන Service එක -->
        <service
            android:name=".PingService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Control Panel (Quick Settings) tile එක - App එක විවෘත නොකර Start/Stop -->
        <service
            android:name=".PingTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <!-- Android 14 සඳහා ආරක්ෂිත පිටුබලය (තිරයේ නොපෙනේ) -->
        <activity
            android:name=".TileStartActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:noHistory="true"
            android:stateNotNeeded="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay" />
    </application>

</manifest>
